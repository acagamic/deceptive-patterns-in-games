---
const nav = [
  { href: "/patterns", label: "Patterns" },
  { href: "/families", label: "Mechanisms" },
  { href: "/harms", label: "Harms" },
  { href: "/rubric", label: "When is it harmful?" },
  { href: "/glossary", label: "Glossary" },
  { href: "/references", label: "References" },
  { href: "/about", label: "About" },
];
const path = Astro.url.pathname.replace(/\/$/, "") || "/";
const isActive = (href: string) =>
  href === "/" ? path === "/" : path === href || path.startsWith(href + "/");
---

<header class="glass-nav">
  <div class="mx-auto flex h-14 max-w-6xl items-center justify-between gap-4 px-4">
    <a
      href="/"
      class="font-sans text-base font-semibold tracking-tight text-ink whitespace-nowrap flex items-center gap-1.5 btn-press-active transition-transform"
    >
      <span>Deceptive Patterns</span><span class="text-accent">in Games</span>
    </a>

    <nav class="hidden items-center gap-1 text-xs lg:flex" aria-label="Primary">
      {
        nav.map((n) => (
          <a
            href={n.href}
            class={`px-3 py-1 transition-colors duration-150 hover:text-ink ${
              isActive(n.href) ? "font-medium text-accent" : "text-muted"
            }`}
          >
            {n.label}
          </a>
        ))
      }
    </nav>

    <div class="flex items-center gap-1">
      <a
        href="/search"
        aria-label="Search"
        class="rounded-full p-2 text-muted transition-colors hover:text-ink btn-press-active"
      >
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true"><circle cx="11" cy="11" r="7"></circle><path d="m21 21-4.3-4.3"></path></svg>
      </a>
      <button
        id="theme-toggle"
        aria-label="Toggle dark mode"
        class="rounded-full p-2 text-muted transition-colors hover:text-ink btn-press-active"
      >
        <svg class="hidden dark:block" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"></path></svg>
        <svg class="block dark:hidden" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true"><path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8z"></path></svg>
      </button>
    </div>
  </div>

  <nav
    class="flex gap-1 overflow-x-auto border-t border-border/60 px-2 py-1.5 text-[11px] lg:hidden"
    aria-label="Primary mobile"
  >
    {
      nav.map((n) => (
        <a
          href={n.href}
          class={`px-3 py-1 whitespace-nowrap transition-colors hover:text-ink ${
            isActive(n.href) ? "font-medium text-accent" : "text-muted"
          }`}
        >
          {n.label}
        </a>
      ))
    }
  </nav>
</header>

<script is:inline>
  (function () {
    var btn = document.getElementById("theme-toggle");
    if (!btn) return;
    btn.addEventListener("click", function () {
      var root = document.documentElement;
      root.classList.toggle("dark");
      try {
        localStorage.setItem(
          "theme",
          root.classList.contains("dark") ? "dark" : "light",
        );
      } catch (e) {}
    });
  })();
</script>
